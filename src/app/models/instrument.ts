export class Instrument {
  constructor(
    instrumentId: string,
    description: string,
    externalIdType: string,
    externalId: string,
    categoryId: string,
    minQuantity: number,
    maxQuantity: number
  ) {}
}
