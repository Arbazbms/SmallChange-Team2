export interface Trade {
    instrumentId: string
    quantity: number
    executionPrice: number
    direction: string
    clientId: string
    order: string
    tradeId: string
    cashValue:number
}
